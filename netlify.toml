[build]
  base = "frontend"  # Netlify ejecutará comandos DENTRO de esta carpeta
  command = "npm ci --production=false && npm run build"
  publish = "netlify-build"  # Ruta RELATIVA al base directory (sin "frontend/")

[context.production.environment]
  NODE_VERSION = "20.12.2"
[build]
  base = "."  # Netlify trabaja desde la raíz
  command = "cd frontend && npm ci --production=false && npm run build"
  publish = "frontend/netlify-build"  # Ruta exacta

[context.production.environment]
  NODE_VERSION = "20.12.2"
[build]
  base = "frontend"
  command = "npm ci --production=false && npm run build"
  publish = "frontend/netlify-build" # Ruta exacta desde la raíz del repo

[context.production.environment]
  NODE_VERSION = "20.12.2"